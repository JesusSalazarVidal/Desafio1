<?php View::content() ?>
<h3>Crear Abogado<h3>

<?= Form::open(); // por defecto llama a la misma url ?> 

      <label>Nombre
      <?= Form::text('abogados.nombre') ?></label>

      

      <?= Form::submit('Agregar') ?> 
      <?= Html::linkAction("listar ", 'Listado') ?> 

<?= Form::close() ?> 