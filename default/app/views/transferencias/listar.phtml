
  <?php View::content() ?> 
  <h3>Transferencias</h3>
  <!--
  <ul>
     
  <?php foreach($listRegistros->items as $item) : ?> 
  <li>
    <strong><?= $item->id ?> - <?= $item->nombre ?></strong> 
    <?= Html::linkAction("editar/$item->id/", 'Editar') ?> 
    <?= Html::linkAction("eliminar/$item->id/", 'Borrar') ?> 
    <?= Html::linkAction("ver/$item->id/", 'Ver') ?> 
      
  </li>
  <?php endforeach ?>
  </ul>
     !-->
  <table BORDER  WIDTH='500'>

  <tr>

    <th>ID</th>

    <th>Cantidad</th>

    <th>Acciones</th>

  </tr>
  <?php foreach($listRegistros->items as $item) : ?> 

  <tr>

    <td><?= $item->id ?></td>

    <td><?= $item->nombre ?></td>

    <td>
    <?= Html::linkAction("editar/$item->id/", 'Editar |') ?> 
    <?= Html::linkAction("eliminar/$item->id/", 'Borrar |') ?> 
    <?= Html::linkAction("ver/$item->id/", 'Ver') ?> 
    </td>

  </tr>
  <?php endforeach ?>

  
</table>


  
   
  <?php if($listRegistros->prev) echo Html::linkAction("index/$listRegistros->prev/", '<< Anterior |') ?> 
  <?php if($listRegistros->next) echo Html::linkAction("index/$listRegistros->next/", 'Proximo >>') ?> 
</div>